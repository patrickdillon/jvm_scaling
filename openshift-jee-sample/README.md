Based on https://github.com/danmcp/openshift-jee-sample
To build `mvn clean package -Popenshift`
You would then need to build container images and change the demployment yaml files to use those images.